# 階段 0 · 截圖 POC 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **這一階段有多個步驟需要人在實機上操作**（標 👤）。代理執行時遇到 👤 步驟要停下來請使用者操作並回報結果，不可自行假設結果。

**Goal:** 在 Android 實機上回答規格第十二節的問題（P-1 JS canvas 截圖、P-2 PixelCopy、P-3 InnerTube）以及附帶的待實測項目，把結果寫回規格。

**Architecture:** 一個一次性的 Views 型 Android app（`poc/android-capture/`），單一 Activity：上方 WebView 載入 YouTube，按鈕觸發各項探測，結果印在畫面上的 log。
POC 程式碼驗證完即刪除（留在 git 歷史），**不進 `android/`**。

**Tech Stack:** Kotlin、Android Views、WebView、`PixelCopy`、`HttpURLConnection`、`androidx.sqlite:sqlite-bundled:2.7.0`。

**Spec:** [`../specs/2026-09-10-yt-space-app-design.md`](../specs/2026-09-10-yt-space-app-design.md) 第二節第 4、5、7 點，第五節【截圖】，第十一節，第十二節。
**路線圖：** [`2026-09-11-yt-space-android-實作計畫.md`](2026-09-11-yt-space-android-實作計畫.md) 階段 0。

## Global Constraints

- POC 的 applicationId：`com.scottchayaa.ytspace.poc`；minSdk 26
- 實機結果才算數，模擬器不算（規格第十二節）
- 通過標準：一般公開與不公開影片都截得到畫面、無 UI 疊加、暫停後秒數誤差 ≤ 0.1 秒
- 測試影片：一般公開 `aqz-KE-bpKQ`、不公開 `KUdmrPVssFA`（`src/lib/storyboard.test.ts` 的實測影片）；
  禁止嵌入、有廣告、Shorts 三種由執行者當場挑選，把 ID 記進結果檔
- commit message 用繁體中文，格式 `類型(範圍): 描述`，不加 AI 生成標記

## 檔案結構

```
poc/android-capture/                       # Android Studio「Empty Views Activity」範本產生
├── RESULTS.md                             # 實測紀錄（T0.9 寫回規格後隨目錄刪除）
└── app/
    ├── build.gradle.kts                   # 加 sqlite-bundled 相依
    └── src/main/
        ├── AndroidManifest.xml            # 加 INTERNET 權限
        └── java/com/scottchayaa/ytspace/poc/
            ├── MainActivity.kt            # 版面、WebView、兩種截圖
            ├── FrameCheck.kt              # 亮度統計與黑畫面判定
            ├── Http.kt                    # GET／POST 並量測實際傳輸量
            ├── JsonExtract.kt             # 從 HTML 擷取 ytInitialPlayerResponse
            ├── PlayerResponse.kt          # 摘要 playability／videoDetails／microformat／storyboard
            ├── WatchPageProbe.kt          # watch page 探測
            ├── InnerTubeProbe.kt          # P-3
            └── Fts5Probe.kt               # FTS5 trigram 與 VACUUM INTO
```

---

### Task 0.1: 開發環境 👤

**Files:** 無（本機環境）

目前這台電腦有 Java 17，但沒有 Android SDK、Android Studio、adb。

- [ ] **Step 1: 👤 安裝 Android Studio**

  從 <https://developer.android.com/studio> 下載最新穩定版並安裝。第一次啟動時選 Standard 安裝，讓它下載 Android SDK、SDK Platform-Tools 與模擬器映像。

- [ ] **Step 2: 👤 設定環境變數**

  Windows「編輯系統環境變數」→ 使用者變數：
  - 新增 `ANDROID_HOME` = `%LOCALAPPDATA%\Android\Sdk`
  - `Path` 加一行 `%LOCALAPPDATA%\Android\Sdk\platform-tools`

  重開終端機。

- [ ] **Step 3: 👤 手機開啟 USB 偵錯**

  手機「設定 → 關於手機」連點「版本號碼」7 次 → 回「設定 → 系統 → 開發人員選項」→ 開啟「USB 偵錯」。用 USB 接上電腦，手機跳出「允許 USB 偵錯？」選允許。

- [ ] **Step 4: 驗證**

  Run: `adb devices`
  Expected: 列出一台狀態為 `device` 的裝置（不是 `unauthorized`）。

---

### Task 0.2: POC 專案骨架

**Files:**
- Create: `poc/android-capture/`（Android Studio 產生）
- Modify: `poc/android-capture/app/build.gradle.kts`
- Modify: `poc/android-capture/app/src/main/AndroidManifest.xml`

- [ ] **Step 1: 👤 以範本建立專案**

  Android Studio → New Project → **Empty Views Activity**：
  - Name：`yt-space-poc`
  - Package name：`com.scottchayaa.ytspace.poc`
  - Save location：`<repo>\poc\android-capture`
  - Language：Kotlin
  - Minimum SDK：API 26
  - Build configuration language：Kotlin DSL

- [ ] **Step 2: 加入 SQLite 自帶驅動相依**

  在 `poc/android-capture/app/build.gradle.kts` 的 `dependencies { … }` 區塊最後加一行：

  ```kotlin
  implementation("androidx.sqlite:sqlite-bundled:2.7.0")
  ```

- [ ] **Step 3: 加入網路權限**

  在 `poc/android-capture/app/src/main/AndroidManifest.xml` 的 `<manifest>` 之下、`<application>` 之前加：

  ```xml
  <uses-permission android:name="android.permission.INTERNET" />
  ```

- [ ] **Step 4: 建置並安裝範本**

  Run: `cd poc/android-capture && ./gradlew installDebug`
  Expected: `BUILD SUCCESSFUL`，手機上出現 `yt-space-poc` 並顯示範本的 Hello World。

- [ ] **Step 5: Commit**

  ```bash
  git add poc/android-capture
  git commit -m "chore(poc): 建立階段 0 截圖 POC 專案骨架"
  ```

  確認 `git status` 沒有把 `local.properties`、`.gradle/`、`build/` 加進去（範本自帶的 `.gitignore` 應已排除）。

---

### Task 0.3: POC 程式碼

**Files:**
- Create: `poc/android-capture/app/src/main/java/com/scottchayaa/ytspace/poc/FrameCheck.kt`
- Create: `…/poc/Http.kt`、`…/poc/JsonExtract.kt`、`…/poc/PlayerResponse.kt`
- Create: `…/poc/WatchPageProbe.kt`、`…/poc/InnerTubeProbe.kt`、`…/poc/Fts5Probe.kt`
- Modify（整檔取代）: `…/poc/MainActivity.kt`

**Interfaces:**
- Produces: 畫面上的按鈕 `Embed`、`Mobile`、`播放`、`跳到10s`、`JS截圖`、`PixelCopy`、`WatchPage`、`InnerTube`、`FTS5`，後續任務依按鈕名稱操作

- [ ] **Step 1: 建立 `FrameCheck.kt`**

```kotlin
package com.scottchayaa.ytspace.poc

import android.graphics.Bitmap
import kotlin.math.sqrt

/** 亮度統計。isBlack 的門檻是暫定值，實測後依紀錄調整並寫回規格第五節。 */
object FrameCheck {
    data class Luma(val mean: Double, val std: Double) {
        val isBlack: Boolean get() = mean < 16.0 && std < 8.0
    }

    fun luma(bmp: Bitmap): Luma {
        val stepX = maxOf(1, bmp.width / 64)
        val stepY = maxOf(1, bmp.height / 36)
        var n = 0
        var sum = 0.0
        var sumSq = 0.0
        var y = 0
        while (y < bmp.height) {
            var x = 0
            while (x < bmp.width) {
                val p = bmp.getPixel(x, y)
                val l = 0.299 * ((p shr 16) and 0xFF) + 0.587 * ((p shr 8) and 0xFF) + 0.114 * (p and 0xFF)
                sum += l
                sumSq += l * l
                n++
                x += stepX
            }
            y += stepY
        }
        val mean = sum / n
        return Luma(mean, sqrt(maxOf(0.0, sumSq / n - mean * mean)))
    }
}
```

- [ ] **Step 2: 建立 `Http.kt`**

```kotlin
package com.scottchayaa.ytspace.poc

import java.io.IOException
import java.net.HttpURLConnection
import java.net.URL
import java.util.zip.GZIPInputStream

object Http {
    const val UA =
        "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/128.0.0.0 Safari/537.36"

    /** 回傳（解壓後的內容, 實際傳輸的位元組數）。 */
    fun get(url: String): Pair<String, Int> = request(url, null)

    fun postJson(url: String, body: String): Pair<String, Int> = request(url, body)

    private fun request(url: String, body: String?): Pair<String, Int> {
        val conn = URL(url).openConnection() as HttpURLConnection
        conn.setRequestProperty("User-Agent", UA)
        conn.setRequestProperty("Accept-Language", "zh-TW,zh;q=0.9,en;q=0.8")
        // 明確要求 gzip：HttpURLConnection 就不會自動解壓，才量得到實際傳輸量
        conn.setRequestProperty("Accept-Encoding", "gzip")
        if (body != null) {
            conn.requestMethod = "POST"
            conn.doOutput = true
            conn.setRequestProperty("Content-Type", "application/json")
            conn.outputStream.use { it.write(body.toByteArray()) }
        }
        val code = conn.responseCode
        val stream = if (code in 200..299) conn.inputStream else conn.errorStream
        val raw = stream?.use { it.readBytes() } ?: ByteArray(0)
        val text = if (conn.contentEncoding == "gzip") {
            GZIPInputStream(raw.inputStream()).bufferedReader().use { it.readText() }
        } else {
            String(raw)
        }
        if (code !in 200..299) throw IOException("HTTP $code: ${text.take(200)}")
        return text to raw.size
    }
}
```

- [ ] **Step 3: 建立 `JsonExtract.kt`**

```kotlin
package com.scottchayaa.ytspace.poc

/** 從 HTML 裡找出 marker 之後的第一個完整 JSON 物件（括號配對，略過字串內容）。 */
object JsonExtract {
    fun objectAfter(text: String, marker: String): String? {
        val at = text.indexOf(marker)
        if (at < 0) return null
        val start = text.indexOf('{', at)
        if (start < 0) return null
        var depth = 0
        var inString = false
        var escaped = false
        for (i in start until text.length) {
            val c = text[i]
            if (inString) {
                when {
                    escaped -> escaped = false
                    c == '\\' -> escaped = true
                    c == '"' -> inString = false
                }
            } else {
                when (c) {
                    '"' -> inString = true
                    '{' -> depth++
                    '}' -> {
                        depth--
                        if (depth == 0) return text.substring(start, i + 1)
                    }
                }
            }
        }
        return null
    }
}
```

- [ ] **Step 4: 建立 `PlayerResponse.kt`**

```kotlin
package com.scottchayaa.ytspace.poc

import org.json.JSONObject

/** 印出規格第二節第 4 點要確認的欄位。 */
object PlayerResponse {
    fun summarize(r: JSONObject): String {
        val ps = r.optJSONObject("playabilityStatus")
        val vd = r.optJSONObject("videoDetails")
        val mf = r.optJSONObject("microformat")?.optJSONObject("playerMicroformatRenderer")
        val spec = r.optJSONObject("storyboards")
            ?.optJSONObject("playerStoryboardSpecRenderer")
            ?.optString("spec")
        return buildString {
            appendLine("  playability=${ps?.optString("status")} reason=${ps?.optString("reason")}")
            appendLine(
                "  videoDetails: title=${vd?.optString("title")} author=${vd?.optString("author")} " +
                    "lengthSeconds=${vd?.optString("lengthSeconds")} isLive=${vd?.optBoolean("isLiveContent")}"
            )
            appendLine(
                "  microformat: publishDate=${mf?.optString("publishDate")} " +
                    "uploadDate=${mf?.optString("uploadDate")} isUnlisted=${mf?.opt("isUnlisted")}"
            )
            append("  storyboard spec: ${spec?.take(80) ?: "（無）"}")
        }
    }
}
```

- [ ] **Step 5: 建立 `WatchPageProbe.kt`**

```kotlin
package com.scottchayaa.ytspace.poc

import org.json.JSONObject

object WatchPageProbe {
    /** InnerTube 探測要沿用 watch page 上的 client 版本與 API key。 */
    @Volatile var clientVersion: String? = null
    @Volatile var apiKey: String? = null

    fun run(videoId: String): String {
        val (html, wireBytes) = Http.get("https://www.youtube.com/watch?v=$videoId")
        clientVersion = Regex("\"INNERTUBE_CLIENT_VERSION\":\"([^\"]+)\"").find(html)?.groupValues?.get(1)
        apiKey = Regex("\"INNERTUBE_API_KEY\":\"([^\"]+)\"").find(html)?.groupValues?.get(1)
        val json = JsonExtract.objectAfter(html, "ytInitialPlayerResponse = ")
            ?: return "WatchPage：找不到 ytInitialPlayerResponse（傳輸 ${wireBytes}B, html ${html.length} 字元）"
        return "WatchPage 傳輸=${wireBytes}B html=${html.length}字元 clientVersion=$clientVersion\n" +
            PlayerResponse.summarize(JSONObject(json))
    }
}
```

- [ ] **Step 6: 建立 `InnerTubeProbe.kt`**

```kotlin
package com.scottchayaa.ytspace.poc

import org.json.JSONObject

/** P-3：youtubei/v1/player 是否帶 storyboard spec、流量多大。 */
object InnerTubeProbe {
    fun run(videoId: String): String {
        val version = WatchPageProbe.clientVersion
            ?: return "InnerTube：請先按 WatchPage 取得 clientVersion"
        val client = JSONObject()
            .put("clientName", "WEB")
            .put("clientVersion", version)
            .put("hl", "zh-TW")
        val body = JSONObject()
            .put("videoId", videoId)
            .put("context", JSONObject().put("client", client))
            .toString()
        val keyParam = WatchPageProbe.apiKey?.let { "&key=$it" } ?: ""
        val (text, wireBytes) = Http.postJson(
            "https://www.youtube.com/youtubei/v1/player?prettyPrint=false$keyParam",
            body
        )
        return "InnerTube 傳輸=${wireBytes}B json=${text.length}字元\n" + PlayerResponse.summarize(JSONObject(text))
    }
}
```

- [ ] **Step 7: 建立 `Fts5Probe.kt`**

```kotlin
package com.scottchayaa.ytspace.poc

import androidx.sqlite.driver.bundled.BundledSQLiteDriver
import androidx.sqlite.execSQL
import java.io.File

/** 規格第二節第 7 點：自帶 SQLite 是否含 FTS5 trigram、是否支援 VACUUM INTO。 */
object Fts5Probe {
    fun run(dir: File): String {
        val db = File(dir, "fts5-probe.db").apply { delete() }
        val snapshot = File(dir, "fts5-snapshot.db").apply { delete() }
        val conn = BundledSQLiteDriver().open(db.path)
        try {
            val version = conn.prepare("SELECT sqlite_version()").use { it.step(); it.getText(0) }
            conn.execSQL("CREATE VIRTUAL TABLE t USING fts5(body, tokenize='trigram')")
            conn.execSQL("INSERT INTO t(body) VALUES ('加勒比海夜潛看到的大蝦')")
            val three = conn.prepare("SELECT count(*) FROM t WHERE t MATCH '夜潛看'").use { it.step(); it.getLong(0) }
            val two = conn.prepare("SELECT count(*) FROM t WHERE t MATCH '大蝦'").use { it.step(); it.getLong(0) }
            conn.execSQL("VACUUM INTO '${snapshot.path}'")
            return "FTS5 sqlite=$version trigram：3 字命中=$three（預期 1）、2 字命中=$two（預期 0）；" +
                "VACUUM INTO 快照=${snapshot.length()}B"
        } finally {
            conn.close()
        }
    }
}
```

- [ ] **Step 8: 整檔取代 `MainActivity.kt`**

```kotlin
package com.scottchayaa.ytspace.poc

import android.annotation.SuppressLint
import android.graphics.Bitmap
import android.graphics.BitmapFactory
import android.graphics.Rect
import android.os.Build
import android.os.Bundle
import android.os.Handler
import android.os.Looper
import android.util.Base64
import android.view.PixelCopy
import android.view.ViewGroup
import android.webkit.WebChromeClient
import android.webkit.WebView
import android.webkit.WebViewClient
import android.widget.Button
import android.widget.EditText
import android.widget.HorizontalScrollView
import android.widget.ImageView
import android.widget.LinearLayout
import android.widget.ScrollView
import android.widget.TextView
import androidx.appcompat.app.AppCompatActivity
import org.json.JSONObject
import org.json.JSONTokener
import java.io.ByteArrayOutputStream
import kotlin.concurrent.thread

/** 階段 0 的一次性 POC：驗證截圖、watch page、InnerTube、FTS5。驗證完即刪除。 */
class MainActivity : AppCompatActivity() {

    private lateinit var webView: WebView
    private lateinit var idInput: EditText
    private lateinit var preview: ImageView
    private lateinit var logView: TextView

    @SuppressLint("SetJavaScriptEnabled")
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        WebView.setWebContentsDebuggingEnabled(true)
        val width = resources.displayMetrics.widthPixels
        val root = LinearLayout(this).apply {
            orientation = LinearLayout.VERTICAL
            fitsSystemWindows = true
        }
        idInput = EditText(this).apply {
            setText("aqz-KE-bpKQ")
            hint = "videoId"
        }
        root.addView(idInput)
        root.addView(
            buttonRow(
                "Embed" to ::loadEmbed,
                "Mobile" to ::loadMobile,
                "播放" to { js("document.querySelector('video').play()") },
                "跳到10s" to { js("var v=document.querySelector('video');v.currentTime=10;v.pause()") },
            )
        )
        root.addView(
            buttonRow(
                "JS截圖" to ::captureJs,
                "PixelCopy" to ::capturePixelCopy,
                "WatchPage" to { probe { WatchPageProbe.run(videoId()) } },
                "InnerTube" to { probe { InnerTubeProbe.run(videoId()) } },
                "FTS5" to { probe { Fts5Probe.run(cacheDir) } },
            )
        )
        webView = WebView(this).apply {
            settings.javaScriptEnabled = true
            settings.domStorageEnabled = true
            settings.mediaPlaybackRequiresUserGesture = false
            webViewClient = WebViewClient()
            webChromeClient = WebChromeClient()
        }
        root.addView(webView, LinearLayout.LayoutParams(width, width * 9 / 16))
        preview = ImageView(this)
        root.addView(preview, LinearLayout.LayoutParams(width / 2, width * 9 / 32))
        logView = TextView(this).apply {
            textSize = 11f
            setTextIsSelectable(true)
        }
        root.addView(
            ScrollView(this).apply { addView(logView) },
            LinearLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, 0, 1f)
        )
        setContentView(root)
    }

    private fun buttonRow(vararg buttons: Pair<String, () -> Unit>): HorizontalScrollView {
        val row = LinearLayout(this)
        for ((label, action) in buttons) {
            row.addView(Button(this).apply {
                text = label
                isAllCaps = false
                setOnClickListener { action() }
            })
        }
        return HorizontalScrollView(this).apply { addView(row) }
    }

    private fun videoId(): String = idInput.text.toString().trim()

    private fun log(message: String) = runOnUiThread { logView.append(message + "\n") }

    private fun js(code: String) = webView.evaluateJavascript(code, null)

    private fun probe(block: () -> String) {
        thread {
            val out = try {
                block()
            } catch (e: Exception) {
                "ERROR ${e::class.simpleName}: ${e.message}"
            }
            log(out)
        }
    }

    // ---- 載入方式（P-1 要比較這兩種）----

    private fun loadEmbed() {
        val url = "https://www.youtube.com/embed/${videoId()}?playsinline=1&controls=0&cc_load_policy=0&rel=0"
        webView.loadUrl(url, mapOf("Referer" to "https://$packageName"))
        log("載入 embed：$url")
    }

    private fun loadMobile() {
        webView.loadUrl("https://m.youtube.com/watch?v=${videoId()}")
        log("載入 m.youtube.com")
    }

    // ---- P-1：JS canvas ----

    private val captureScript = """
        (function(){
          var v = document.querySelector('video');
          if (!v) return JSON.stringify({error: 'no video element'});
          v.pause();
          var ad = !!document.querySelector('.ad-showing');
          var c = document.createElement('canvas');
          c.width = v.videoWidth; c.height = v.videoHeight;
          try {
            c.getContext('2d').drawImage(v, 0, 0, c.width, c.height);
            return JSON.stringify({t: v.currentTime, ad: ad, data: c.toDataURL('image/jpeg', 0.92)});
          } catch (e) {
            return JSON.stringify({error: String(e), t: v.currentTime, ad: ad});
          }
        })()
    """.trimIndent()

    private fun captureJs() {
        val started = System.nanoTime()
        webView.evaluateJavascript(captureScript) { raw ->
            val ms = (System.nanoTime() - started) / 1_000_000
            val value = JSONTokener(raw).nextValue()
            if (value !is String) {
                log("JS截圖：腳本回傳 $raw")
                return@evaluateJavascript
            }
            val json = JSONObject(value)
            if (json.has("error")) {
                log("JS截圖失敗：$json")
                return@evaluateJavascript
            }
            val bytes = Base64.decode(json.getString("data").substringAfter(","), Base64.DEFAULT)
            val bmp = BitmapFactory.decodeByteArray(bytes, 0, bytes.size)
            report("JS", bmp, json.getDouble("t"), json.getBoolean("ad"), ms)
        }
    }

    // ---- P-2：PixelCopy ----

    private val rectScript = """
        (function(){
          var v = document.querySelector('video');
          if (!v) return JSON.stringify({error: 'no video element'});
          v.pause();
          var r = v.getBoundingClientRect();
          return JSON.stringify({x: r.left, y: r.top, w: r.width, h: r.height,
            dpr: window.devicePixelRatio, t: v.currentTime, ad: !!document.querySelector('.ad-showing')});
        })()
    """.trimIndent()

    private fun capturePixelCopy() {
        webView.evaluateJavascript(rectScript) { raw ->
            val value = JSONTokener(raw).nextValue()
            if (value !is String) {
                log("PixelCopy：腳本回傳 $raw")
                return@evaluateJavascript
            }
            val j = JSONObject(value)
            if (j.has("error")) {
                log("PixelCopy：$j")
                return@evaluateJavascript
            }
            val dpr = j.getDouble("dpr")
            val loc = IntArray(2).also { webView.getLocationInWindow(it) }
            val left = loc[0] + (j.getDouble("x") * dpr).toInt()
            val top = loc[1] + (j.getDouble("y") * dpr).toInt()
            val src = Rect(left, top, left + (j.getDouble("w") * dpr).toInt(), top + (j.getDouble("h") * dpr).toInt())
            // 影片可能超出 WebView 可見範圍，裁到 WebView 內
            if (!src.intersect(Rect(loc[0], loc[1], loc[0] + webView.width, loc[1] + webView.height))) {
                log("PixelCopy：影片不在可見範圍")
                return@evaluateJavascript
            }
            val bmp = Bitmap.createBitmap(src.width(), src.height(), Bitmap.Config.ARGB_8888)
            val started = System.nanoTime()
            PixelCopy.request(window, src, bmp, { result ->
                val ms = (System.nanoTime() - started) / 1_000_000
                if (result == PixelCopy.SUCCESS) {
                    report("PixelCopy", bmp, j.getDouble("t"), j.getBoolean("ad"), ms)
                } else {
                    log("PixelCopy 失敗 code=$result")
                }
            }, Handler(Looper.getMainLooper()))
        }
    }

    // ---- 共用：縮成 320×180 WebP、統計亮度 ----

    private fun report(kind: String, bmp: Bitmap, t: Double, ad: Boolean, ms: Long) {
        val stats = FrameCheck.luma(bmp)
        val thumb = Bitmap.createScaledBitmap(bmp, 320, 180, true)
        val out = ByteArrayOutputStream()
        @Suppress("DEPRECATION")
        val format = if (Build.VERSION.SDK_INT >= 30) Bitmap.CompressFormat.WEBP_LOSSY else Bitmap.CompressFormat.WEBP
        thumb.compress(format, 75, out)
        preview.setImageBitmap(thumb)
        log(
            "$kind ${bmp.width}x${bmp.height} t=${"%.3f".format(t)} ad=$ad " +
                "亮度平均=${"%.1f".format(stats.mean)} 標準差=${"%.1f".format(stats.std)} 黑畫面=${stats.isBlack} " +
                "webp=${out.size()}B 耗時=${ms}ms"
        )
    }
}
```

- [ ] **Step 9: 建置並安裝**

  Run: `cd poc/android-capture && ./gradlew installDebug`
  Expected: `BUILD SUCCESSFUL`；手機上開啟後看得到兩列按鈕、黑色的 WebView 區、下方空白 log。

- [ ] **Step 10: 冒煙測試**

  👤 按【FTS5】。
  Expected: log 出現一行 `FTS5 sqlite=…`，或一行 `ERROR …: no such module: fts5`（也是有效結果，代表 R-2 成立）。

- [ ] **Step 11: Commit**

  ```bash
  git add poc/android-capture
  git commit -m "feat(poc): 截圖、watch page、InnerTube、FTS5 探測程式"
  ```

---

### Task 0.4: 建立結果紀錄檔，實測 watch page 與 InnerTube

**Files:**
- Create: `poc/android-capture/RESULTS.md`

- [ ] **Step 1: 建立紀錄檔**

````markdown
# 階段 0 POC 結果（暫存；T0.9 寫回規格後隨 poc/ 一併刪除）

- 裝置：型號／Android 版本／Android System WebView 版本（設定 → 應用程式 → Android System WebView）
- 日期：

## 測試影片

| 類型 | videoId | 備註 |
|---|---|---|
| 一般公開 | aqz-KE-bpKQ | |
| 不公開 | KUdmrPVssFA | |
| 禁止嵌入 | | |
| 有廣告 | | |
| Shorts | | |

## watch page 與 InnerTube（P-3）

| 類型 | watch 傳輸 B | playability | publishDate | uploadDate | isUnlisted | 有 spec | InnerTube 傳輸 B | InnerTube 有 spec |
|---|---|---|---|---|---|---|---|---|

## 截圖（P-1 JS canvas／P-2 PixelCopy）

| 類型 | 載入方式 | 方法 | 解析度 | 亮度平均／標準差 | 黑畫面 | 畫面上有 UI 疊加？ | ad | 同一暫停點截兩次 t 相同？ | 跳到 10s 後 t | 耗時 ms | webp B |
|---|---|---|---|---|---|---|---|---|---|---|---|

## embed 能否播放（R-5）

| 類型 | embed 能播？錯誤訊息 | m.youtube.com 能播？ |
|---|---|---|

## FTS5

- sqlite 版本：
- trigram 3 字命中／2 字命中：
- VACUUM INTO：

## 結論（T0.8 填寫）

- P-1：
- P-2：
- P-3：
- 黑畫面門檻：
- FTS5：
- 播放器載入方式：
````

- [ ] **Step 2: 👤 對五支測試影片各跑一次【WatchPage】→【InnerTube】**

  每支影片：在輸入框換 videoId → 按【WatchPage】→ 等 log 出現 → 按【InnerTube】。把 log 的數字填進「watch page 與 InnerTube」表。
  Expected：一般公開影片 `playability=OK` 且 `storyboard spec` 有值；不公開影片 `isUnlisted=true`。

- [ ] **Step 3: 👤 跑一次【FTS5】，填進 FTS5 段落**

- [ ] **Step 4: Commit**

  ```bash
  git add poc/android-capture/RESULTS.md
  git commit -m "docs(poc): 記錄 watch page、InnerTube、FTS5 實測結果"
  ```

---

### Task 0.5: 實測 P-1（JS canvas 截圖）與 embed 可播性 👤

**Files:**
- Modify: `poc/android-capture/RESULTS.md`

每支測試影片、每種載入方式（【Embed】與【Mobile】）各做一輪：

- [ ] **Step 1: 👤 載入並播放**

  換 videoId → 按【Embed】（或【Mobile】）→ 等頁面載入 → 按【播放】（或直接點影片）。
  embed 若顯示錯誤（例如「影片無法播放」、錯誤 153、「已停用在其他網站播放」），把訊息記進「embed 能否播放」表，這一輪改用【Mobile】。

- [ ] **Step 2: 👤 播放中按【JS截圖】**

  Expected（通過）：左下預覽出現畫面、`黑畫面=false`、解析度等於影片原始解析度、預覽上**沒有**播放器控制列或字幕。
  失敗型態要照實記錄：`JS截圖失敗：{"error":"SecurityError…"}`（canvas 被 taint）、`黑畫面=true`、或預覽有 UI。

- [ ] **Step 3: 👤 不按播放，再按一次【JS截圖】**

  Expected：`t` 與上一次相同（暫停點不動），亮度統計幾乎相同。記進「同一暫停點截兩次 t 相同？」。

- [ ] **Step 4: 👤 按【跳到10s】→ 等 1 秒 →【JS截圖】**

  Expected：`t` 在 9.9～10.1 之間。記進「跳到 10s 後 t」。

- [ ] **Step 5: 👤 在有廣告的影片上，趁廣告播放時按【JS截圖】**

  記錄 `ad=` 是否為 `true`（廣告偵測是否可行）。

- [ ] **Step 6: Commit**

  ```bash
  git add poc/android-capture/RESULTS.md
  git commit -m "docs(poc): 記錄 JS canvas 截圖實測結果"
  ```

---

### Task 0.6: 實測 P-2（PixelCopy）👤

**Files:**
- Modify: `poc/android-capture/RESULTS.md`

- [ ] **Step 1: 👤 重複 Task 0.5 的 Step 1～5，把【JS截圖】換成【PixelCopy】**

  PixelCopy 截的是螢幕上實際畫出來的東西，所以要特別記錄「畫面上有 UI 疊加？」—— embed 的 `controls=0` 應該沒有控制列；
  `m.youtube.com` 可能帶到頁面介面。

- [ ] **Step 2: Commit**

  ```bash
  git add poc/android-capture/RESULTS.md
  git commit -m "docs(poc): 記錄 PixelCopy 實測結果"
  ```

---

### Task 0.7: 黑畫面門檻校準

**Files:**
- Modify: `poc/android-capture/RESULTS.md`

- [ ] **Step 1: 整理亮度數據**

  從截圖表整理兩組數字：成功截圖中「最暗的正常畫面」（例如夜景）的平均與標準差，以及失敗截圖（黑畫面）的平均與標準差。

- [ ] **Step 2: 訂門檻**

  門檻必須把兩組分開：黑畫面組全部判為 `true`、正常組全部判為 `false`。
  若暫定值（平均 < 16 且標準差 < 8）已能分開，沿用；否則取兩組之間的中點。寫進結論「黑畫面門檻」。
  若沒有任何失敗樣本（兩種截圖都從未出現黑畫面），結論寫「未觀察到黑畫面，沿用暫定值」。

- [ ] **Step 3: Commit**

  ```bash
  git add poc/android-capture/RESULTS.md
  git commit -m "docs(poc): 黑畫面判定門檻"
  ```

---

### Task 0.8: 結論

**Files:**
- Modify: `poc/android-capture/RESULTS.md`

- [ ] **Step 1: 依規格第十二節的通過標準判定並填寫結論段落**

  | 項目 | 判定方式 |
  |---|---|
  | P-1 | 一般公開與不公開影片，在至少一種載入方式下：非黑、無 UI 疊加、兩次 t 相同、跳 10s 誤差 ≤ 0.1 → **通過** |
  | P-2 | 同上，改看 PixelCopy |
  | `capture` 實作 | P-1 通過 → JS canvas；否則 P-2 通過 → PixelCopy ＋ 裁切；都不通過 → 只提供相簿選圖 |
  | P-3 | InnerTube 有 spec 且傳輸量明顯小於 watch page → 回填改走 InnerTube；否則維持 watch page |
  | FTS5 | 3 字命中 1、2 字命中 0 → `BundledSQLiteDriver` 可用；`no such module` → 改用 `requery/sqlite-android`（R-2） |
  | 播放器載入方式 | embed 對一般影片可播 → 用 embed（禁止嵌入的影片退到 m.youtube.com）；embed 普遍不可播 → 一律 m.youtube.com（R-5） |

- [ ] **Step 2: 👤 使用者確認結論**

  把結論段落給使用者看，取得同意後再進 Task 0.9。

- [ ] **Step 3: Commit**

  ```bash
  git add poc/android-capture/RESULTS.md
  git commit -m "docs(poc): 階段 0 結論"
  ```

---

### Task 0.9: 結果寫回規格，刪除 POC

**Files:**
- Modify: `docs/superpowers/specs/2026-09-10-yt-space-app-design.md`
- Modify: `docs/superpowers/plans/2026-09-11-yt-space-android-實作計畫.md`
- Delete: `poc/`

- [ ] **Step 1: 依結論改規格**

  | 結論 | 規格要改的地方 |
  |---|---|
  | watch page 欄位 | 第二節第 4 點表格「上傳日期、是否不公開」列：寫上實測到的欄位路徑，移除 ⏳ |
  | watch page 傳輸量 | 第十一節「流量與網路」：以實測值取代「約 250 KB（估計值）」，重算 500 支影片的總量 |
  | P-3 | 第十一節：若採 InnerTube，改寫流程第 2a 步與流量段落；移除 ⏳ |
  | P-1／P-2 | 第二節第 5 點：寫明哪條路可行、哪條不可行及原因；第十二節新增「結果」小節；第五節【截圖】流程依採用的實作調整 |
  | 黑畫面門檻 | 第五節「失敗判定」：寫上門檻數字，移除 ⏳ |
  | 廣告偵測 | 第五節「失敗判定」：`.ad-showing` 是否可靠 |
  | FTS5 | 第二節第 7 點與第三節技術選型「資料庫」列：寫定採用的驅動，移除 ⏳ |
  | 播放器載入方式 | 第三節技術選型「WebView」列：寫定載入方式，移除 ⏳ |
  | 第十六節 | 刪除已解決的列 |

- [ ] **Step 2: 路線圖標記階段 0 完成**

  在路線圖「一、階段總覽」表的階段 0 那列，細節計畫欄改為 `✅ 完成（結論見規格第十二節）`。

- [ ] **Step 3: 刪除 POC**

  Run: `git rm -r -q poc`
  Expected: `git status` 顯示 `poc/` 底下的檔案全部為 deleted。

- [ ] **Step 4: 檢查規格裡沒有殘留的 POC ⏳**

  Run: `grep -n "⏳" docs/superpowers/specs/2026-09-10-yt-space-app-design.md`
  Expected: 不再出現 P-1／P-2／P-3、WebP、FTS5、`publishDate`、黑畫面門檻相關的 ⏳。

- [ ] **Step 5: Commit**

  ```bash
  git add docs/superpowers
  git commit -m "docs(spec): 階段 0 POC 結果寫回規格，移除 POC 程式碼"
  ```
